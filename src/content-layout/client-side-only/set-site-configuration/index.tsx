import React from 'react';
//  hooks
import useSiteConfiguration from '../../../hooks/use-site-configuration';

const SetSiteConfiguration = () => {
  useSiteConfiguration();

  return <></>;
};

export default SetSiteConfiguration;
