// import { Collection } from 'signaldb';
// interfaces and types
import { JobApplication, JobInterview } from './interfaces-and-types';

const getCollections = () => {
  // const applications = new Collection<JobApplication>();
  // const interviews = new Collection<JobInterview>();

  // return { applications, interviews };
};

export default getCollections;
